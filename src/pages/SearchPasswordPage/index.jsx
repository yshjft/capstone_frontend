import React from 'react'
import SearchPasswordContainer from './SearchPasswordContainer'

const SearchPasswordPage = (props) => {
  return <SearchPasswordContainer />
}

export default SearchPasswordPage
