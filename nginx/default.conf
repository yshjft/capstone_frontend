server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location /api {
        proxy_pass http://3.34.142.129:5000/api;
    }

    location / {
        try_files $uri $uri/ /index.html;
        # 왜 안되는 것일까
    }
}